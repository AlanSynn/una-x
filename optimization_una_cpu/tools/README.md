# Packet tools
`audit_checkout.py` records Git state. `probe_laptop.py` records portable CPU/RAM/platform facts. `validate_packet.py` checks the executable handoff references. `verify_prior.py` checks the decisive preserved historical summaries. The pinned-source verifier lives under `prior/una_optimization/verify_repository.py`. These tools do not prove application correctness by themselves.
