# Optimization candidate register

## C1 stable ordered CSR construction
Status entering laptop campaign: candidate, not production-qualified.
Removes repeated per-node full-edge scans and Python list extension. Stable grouping preserves baseline per-row incidence order: all forward incidences in original edge order followed by reverse incidences in original edge order. Original builder is fallback outside exact admitted NumPy domain.
Historical synthetic result: strong component/setup reduction, preserved only as prior evidence. Must be requalified on complete fork, genuine GIS, installed wheel, and laptop resources.

## C2 CPU independent-job scheduling
Status: tuning candidate after genuine L2 and single-job memory measurement.
Variables: worker processes, Numba threads per worker, queue depth, writer concurrency. Must respect total CPU and RAM. Do not assume historical 2x2 result transfers to laptop.

## C3 private search scratch/state reduction
Status: not admitted by default.
May be attempted once only if post-C1 profiling shows material coverage and dossier proof demonstrates all future observations are preserved. Reject on state mismatch, artifact mismatch, inadequate Amdahl coverage, memory regression, or maintenance cost.

## Explicitly rejected/deferred
- scalar immediate Dijkstra relaxation replacing baseline vector mask, due duplicate-neighbor semantic counterexample,
- reassociated/tree reductions,
- replacing K-alternatives with AggregateFlow,
- broad cross-run topology/result caches,
- blind public RunBatch parallelization,
- GPU/native backend work in this campaign,
- further CSR micro-optimization after its residual coverage becomes immaterial.