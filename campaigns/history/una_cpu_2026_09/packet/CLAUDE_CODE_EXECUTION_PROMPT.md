# Claude Code execution constitution

Execute the campaign. Do not return another plan and stop. Ordinary implementation, test, packaging, profiling, and fixture failures are yours to debug. Escalate only when a high-level assumption is false or completing the task requires changing a frozen semantic, numerical, user-facing, scientific, safety, or resource contract.

Target repository: `AlanSynn/una-x`. Upstream provenance/oracle: `City-Form-Lab/urban_network_analysis@c15ebda6981397f46eed5c2d55229f71e57d44fb`. Work from a verified local `main`, then create `perf/una-cpu-throughput` and isolated worktrees. Do not rewrite main, push, merge, release, or alter credentials unless explicitly authorized.

Read START_HERE, DESIGN_AUTHORITY, SOURCE_AUDIT, PLAN, TASKS_CLAUDE, contracts, benchmark/promotion policies, and the relevant dossier. Preserve baseline behavior even when surprising. Keep baseline source independent from candidate code.

Mandatory track: verify, integrate, independently review, or reject the guarded stable ordered-CSR candidate for non-turn accessibility. Build genuine GIS/public-API, chronology/state, flow-gravity-cap, CSV/GeoJSON/Feather, and installed-wheel validation. Tune CPU process/thread/queue/writer concurrency only after the algorithmic candidate is qualified. GPU and new native backends are out of scope.

Evidence must distinguish measured, derived from measured, hypothetical sensitivity analysis, and unavailable. A synthetic component win is not an end-to-end production claim. Timer boundaries include required preparation, execution, synchronization, materialization, and required output. Retain raw samples.

Finish with reviewed immutable source SHA, exact commands/environment, installed-path proof, accepted/rejected register, selected configuration, memory/resource record, scoped performance claims, unavailable gates, independent review status, CI status, and merge manifest. Stop under DECISION_CONTRACT rather than continuing indefinitely.