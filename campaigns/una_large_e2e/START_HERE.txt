You are the execution coordinator for the new UNA large end-to-end CPU campaign. Execute, debug, verify, and produce a finite decision. Do not return another plan and stop. Repository: AlanSynn/una-x. Active packet: campaigns/una_large_e2e. Integration branch: perf/una-large-e2e.

The baseline source is 361928e4ba38f34622cafe065b0025244db61368, including ordered CSR. The packet-publication commit may be newer while src/ remains identical. Record both identities. Do not use pre-CSR code as the primary comparator. The previous campaign is historical under campaigns/history/una_cpu_2026_09/packet; do not resume its DAG or overwrite its evidence.

Read EXECUTION.md, AUTHORITY.md, SOURCE_AUDIT.md, TASKS_CLAUDE.yaml, CONTRACT.md and DECISION.md first. Run tools/validate_packet.py, the packet unittest suite, and tools/verify_source.py --repo <checkout> using paths rooted at this packet. Resolve file paths from the checkout, never by searching the user's home folder. Preserve dirty/untracked work. Work in isolated worktrees from immutable verified SHAs.

Before runtime changes, repair the benchmark harness under HARNESS.md: dispatch must actually execute flow when requested; record/assert installed package identity in every worker; count only successful validated jobs; sample simultaneous recursive process-tree memory; bound queues, timeouts and cancellations; time the real public call without diagnostic extra constructors; separate cold/JIT, warm execution and verification; verify input hashes and real thread pools. Negative harness tests must fail deliberately corrupted runs.

Establish an observed-network baseline using WORKLOADS.md and the pinned input catalog. Generated W1/W3 grids are synthetic GIS fixtures, not observed street data. Use them only for controlled tests. Freeze exact Settings, required outputs, numerical threading partition, resource budget and timing protocol before candidate measurements. No user production target has been supplied; do not invent one.

Only six hypotheses are authorized, and each requires measured admission plus reviewed proof: A1 snapshot-preserving search scratch; A2 destination locality; A3 private scope initialization; F1 nogil with fixed arithmetic stripes; F2 local flow overlap/scratch; F3 gradient chunk memory. Follow each dossier's preconditions, pseudocode, adversarial cases, cleanup rules and rejection criteria. Preserve baseline heap quirks, ordered reductions, state, errors and artifacts. Do not change fastmath, precision, flow model, public defaults, or RunBatch semantics. GPU/native backends and broad caches are out of scope.

One owner per writable scope, one exclusive performance owner, independent review separate from implementation. Read-only proof/fixture work may overlap; heavy execution may not. Infer neither P-core pinning nor flow-thread limits from NUMBA_NUM_THREADS. Use RESOURCES.md and source-specific pool accounting. No repeated worker polling.

Use L0/L1 while editing, observed-data L2 before L3, and only the immutable final candidate for L4. Installed baseline and candidate wheels must both be measured outside checkouts. Record exact commands, source/wheel/cache/fixture identities, raw times, output validation, memory and execution-path proof. Stop at DECISION.md limits. Produce all final artifacts, including explicit blocked/unavailable claims and the merge manifest.

Resolve ordinary failures without approval loops. Reject an unsafe experiment rather than improvising a new algorithm. Do not push, merge main, release, rewrite history, modify credentials/provider settings or discard user files. Prior authorization covered publishing this packet only. Finish with a reviewed local candidate and evidence, or an honest finite no-change/blocked outcome.
